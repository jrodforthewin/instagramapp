@model InstagramAPIApp.Models.UserMediaAndBio

@{
    ViewBag.Title = "Bio";
}

<script>
    userBioAndMedia = @Html.Raw(Json.Encode(Model));
</script>

<div flex layout-fill>
    testes please<h1>shutup</h1>
    <div layout="column" layout-fill>
        <div ng-app="myApp">
            <div ng-controller="GridCtrl as appCtrl">
                @*<md-grid-list md-cols-gt-md="6" md-cols-sm="2" md-cols-md="3"
                              md-row-height-gt-md="4:6"  md-row-height="4:6"
                              md-gutter-gt-md="2px" md-gutter-gt-sm="8px" md-gutter="4px">
                    <md-grid-tile ng-repeat="tile in appCtrl.colorTiles"
                                  @ *ng-style="{
              'background': tile.color
            }"
                                  md-colspan-gt-sm="{{tile.colspan}}"
                                  md-rowspan-gt-sm="{{tile.rowspan}}"* @
                                  >
                        
                        <md-card>
                            <img src="{{tile.url}}" class="md-card-image" alt="{{tile.caption}}">
                            <md-card-content>
                                
                                <p>{{tile.likes}} + {{tile.comments}}</p>
                            </md-card-content>
                            <md-card-footer>
                                {{tile.caption}}
                            </md-card-footer>
                        </md-card>
                    </md-grid-tile>
                </md-grid-list>*@

                        <div class="parent" layout="column" ng-repeat="tile in appCtrl.colorTiles" flex>
                            <md-card>
                                <img src="{{tile.url}}" class="md-card-image" alt="{{tile.caption}}">
                                <md-card-content>

                                    <p>{{tile.likes}} + {{tile.comments}}</p>
                                </md-card-content>
                                <md-card-footer>
                                    {{tile.caption}}
                                </md-card-footer>
                            </md-card>
                        </div>
            </div>

        </div>
    </div>
</div>
